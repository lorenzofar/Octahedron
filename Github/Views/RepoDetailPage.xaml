<Page
    x:Class="Github.Views.RepoDetailPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Github.Views"
    xmlns:controls="using:Template10.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    DataContext="{Binding Source={StaticResource ViewModelLocator}, Path=RepoDetail}"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <controls:PageHeader Frame="{x:Bind Frame}" x:Uid="repoDetail_header" BackButtonVisibility="Collapsed" VisualStateNarrowMinWidth="0" VisualStateNormalMinWidth="720">
        </controls:PageHeader>

        <Pivot Grid.Row="1">
            <PivotItem x:Uid="info_txt" Header="INFO">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition/>
                    </Grid.RowDefinitions>
                    <StackPanel Padding="12" Background="{StaticResource SystemControlBackgroundAccentBrush}">
                        <TextBlock Text="{Binding Path=repo.Name}" HorizontalAlignment="Center" Style="{ThemeResource HeaderTextBlockStyle}"/>
                        <TextBlock Text="{Binding Path=repo.Owner.Login}" HorizontalAlignment="Center" Style="{ThemeResource SubtitleTextBlockStyle}"/>
                        <TextBlock Text="{Binding Path=repo.Language}" HorizontalAlignment="Center"/>
                        <ScrollViewer Margin="12" HorizontalScrollMode="Disabled" VerticalScrollBarVisibility="Auto">
                            <TextBlock HorizontalAlignment="Center" Text="{Binding Path=repo.Description}" MaxHeight="100" TextWrapping="Wrap"/>
                        </ScrollViewer>
                        <Grid Margin="0,6,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition/>
                                <ColumnDefinition/>
                                <ColumnDefinition/>
                            </Grid.ColumnDefinitions>
                            <StackPanel HorizontalAlignment="Center">
                                <TextBlock HorizontalAlignment="Center" Text="{Binding Path=repo.ForksCount}" Style="{ThemeResource TitleTextBlockStyle}"/>
                                <TextBlock HorizontalAlignment="Center" x:Uid="forks_txt"/>
                            </StackPanel>
                            <StackPanel Grid.Column="1" HorizontalAlignment="Center">
                                <TextBlock HorizontalAlignment="Center" Text="{Binding Path=repo.StargazersCount}" Style="{ThemeResource TitleTextBlockStyle}"/>
                                <TextBlock HorizontalAlignment="Center" x:Uid="stars_txt"/>
                            </StackPanel>
                            <StackPanel Grid.Column="2" HorizontalAlignment="Center">
                                <TextBlock HorizontalAlignment="Center" Text="{Binding Path=repo.OpenIssuesCount}" Style="{ThemeResource TitleTextBlockStyle}"/>
                                <TextBlock HorizontalAlignment="Center" x:Uid="issues_txt"/>
                            </StackPanel>
                        </Grid>
                    </StackPanel>
                </Grid>
            </PivotItem>
            <PivotItem Header="ISSUES">
                
            </PivotItem>
            <PivotItem Header="PULL"></PivotItem>
        </Pivot>

        <CommandBar Grid.Row="2">
            
        </CommandBar>

    </Grid>
</Page>
